#include<stdio.h>
void maxMin(int arr[],int size)
{
   
}
int main()
{
    int size;
    int arr[20];
    scanf("%d",&size);
    for (int i = 0; i < size; i++)
    {
        scanf("%d",&arr[i]);
    }
    maxMin(arr,size);  
    
        return 0;
}